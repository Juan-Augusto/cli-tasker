# CLI Tasker Project

## 1. README.md
# 📝 CLI Tasker

A lightweight **Command Line Interface (CLI)** for managing daily tasks. Built with Node.js, this tool allows you to add, update, delete, and track the status of your tasks directly from your terminal.



### 🚀 Installation
1. Clone this repository.
2. Run `npm install`.
3. Run `npm link` to register the `cli-tasker` command globally.

### 🛠 Usage
- **Add:** `cli-tasker add "My task"`
- **List:** `cli-tasker list` (or `cli-tasker list done`)
- **Update:** `cli-tasker update <id> "New content"`
- **Delete:** `cli-tasker delete <id>`
- **Mark:** `cli-tasker mark-done <id>` or `cli-tasker mark-in-progress <id>`

---